import { VowelNumbePipe } from './vowel-numbe.pipe';

describe('VowelNumbePipe', () => {
  it('create an instance', () => {
    const pipe = new VowelNumbePipe();
    expect(pipe).toBeTruthy();
  });
});
